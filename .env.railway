# Railway Environment Variables Template
# Copy this file to Railway's environment variables section

# Database (Railway will provide PostgreSQL URL)
DATABASE_URL="${RAILWAY_DATABASE_URL}"

# Redis (Railway will provide Redis URL if you add Redis service)
REDIS_URL="${RAILWAY_REDIS_URL}"

# Application
NODE_ENV="production"
PORT="${PORT}"  # Railway auto-assigns this

# JWT Secrets (MUST CHANGE IN PRODUCTION)
JWT_SECRET="${JWT_SECRET}"
JWT_REFRESH_SECRET="${JWT_REFRESH_SECRET}"

# Payment Processing (Add your production keys)
SQUARE_ACCESS_TOKEN="${SQUARE_ACCESS_TOKEN}"
STRIPE_SECRET_KEY="${STRIPE_SECRET_KEY}"

# Frontend URL (Update with your Railway app URL)
FRONTEND_URL="https://${RAILWAY_STATIC_URL}"

# API URL (for frontend to connect to backend)
VITE_API_URL="https://${RAILWAY_PUBLIC_DOMAIN}/api"

# Session
SESSION_SECRET="${SESSION_SECRET}"

# Admin User (for initial setup)
ADMIN_USERNAME="${ADMIN_USERNAME}"
ADMIN_PASSWORD_HASH="${ADMIN_PASSWORD_HASH}"

# QuickBooks Integration
QUICKBOOKS_CLIENT_ID="${QUICKBOOKS_CLIENT_ID}"
QUICKBOOKS_CLIENT_SECRET="${QUICKBOOKS_CLIENT_SECRET}"
QUICKBOOKS_REDIRECT_URI="https://${RAILWAY_PUBLIC_DOMAIN}/api/quickbooks/callback"
QUICKBOOKS_ENVIRONMENT="production"  # or "sandbox" for testing

# Security
CORS_ORIGIN="https://${RAILWAY_STATIC_URL}"
SECURE_COOKIES="true"
TRUST_PROXY="true"

# Logging
LOG_LEVEL="info"
AUDIT_LOG_PATH="/app/logs/audit"

# File Storage (if using local storage)
UPLOAD_PATH="/app/uploads"

# Optional: Sentry for error tracking
SENTRY_DSN="${SENTRY_DSN}"

# Optional: Analytics
GOOGLE_ANALYTICS_ID="${GOOGLE_ANALYTICS_ID}"