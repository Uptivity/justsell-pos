# Docker Guide (optional, Windows-friendly)
- Use multi-stage Dockerfiles; docker-compose for app+DB+Redis; healthchecks.
- Example services: mysql:8, redis:7, app containers; named networks; volume mounts.